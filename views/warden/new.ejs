<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Add Warden</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #0bcce5; /* updated background */
            margin: 0;
            padding: 40px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #0b3a47; /* dark teal for good contrast */
        }
        .container {
            background: white;
            padding: 30px 40px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
            max-width: 400px;
            width: 100%;
        }
        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-weight: 700;
            font-size: 1.8rem;
            color: #0b3a47; /* dark teal for heading */
        }
        form {
            display: flex;
            flex-direction: column;
        }
        label {
            font-weight: 600;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 1rem;
            color: #0b3a47; /* dark teal */
        }
        input[type="text"] {
            padding: 12px 15px;
            font-size: 1rem;
            border: 1.5px solid #0bcce5; /* border color matching bg */
            border-radius: 6px;
            margin-bottom: 24px;
            transition: border-color 0.3s ease;
            color: #0b3a47; /* text color */
        }
        input[type="text"]::placeholder {
            color: #7ecfdb; /* lighter placeholder for visibility */
        }
        input[type="text"]:focus {
            border-color: #089abf; /* slightly darker cyan */
            outline: none;
            box-shadow: 0 0 5px #089abf;
            color: #0b3a47;
        }
        .btn-group {
            display: flex;
            gap: 15px;
        }
        button.btn, a.btn {
            flex: 1;
            padding: 12px 0;
            font-weight: 600;
            border-radius: 6px;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            text-align: center;
            user-select: none;
            text-decoration: none;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            gap: 8px;
            color: white;
            transition: background-color 0.3s ease;
        }
        button.btn {
            background-color: #0b82a9; /* darker teal button */
            border: none;
        }
        button.btn:hover {
            background-color: #066a82;
        }
        a.btn.btn-secondary {
            background-color: #647d86; /* muted grey-blue */
        }
        a.btn.btn-secondary:hover {
            background-color: #50666e;
        }
        /* Icons */
        .icon {
            font-size: 1.2rem;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>👮‍♂️ Add New Warden</h1>
    <form action="/warden/create" method="POST">
        <label for="name"><span class="icon">👤</span> Name:</label>
        <input name="name" id="name" type="text" placeholder="Enter warden's name" required />

        <label for="contact"><span class="icon">📞</span> Contact:</label>
        <input name="contact" id="contact" type="text" placeholder="Enter contact number" required />

        <div class="btn-group">
            <button type="submit" class="btn">➕ Add</button>
            <a href="/warden" class="btn btn-secondary">⬅️ Back</a>
        </div>
    </form>
</div>
</body>
</html>
