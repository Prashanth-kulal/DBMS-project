<!DOCTYPE html>
<html>
<head>
  <title>Hostel Management System</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #e0f7fa, #80deea);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .login-box {
      background-color: white;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
      text-align: center;
      max-width: 400px;
      width: 100%;
    }
    select, input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 1rem;
      height: 44px; /* 🔧 Added to make consistent */
      box-sizing: border-box;
    }
    button {
      width: 100%;
      padding: 10px;
      background-color: #00796b;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1rem;
    }
    a {
      color: #00796b;
      text-decoration: none;
    }
  </style>
</head>
<body>

  <div class="login-box">
    <h1 style="color: #00796b; margin-bottom: 10px;">🏠 Welcome to Hostel Management System</h1>
    <p style="color: #555; margin-bottom: 30px;">Please log in to continue</p>

    <h2 style="color: #00796b; margin-bottom: 20px;">Login</h2>

    <% if (error) { %>
      <p style="color: red; margin-bottom: 20px;"><%= error %></p>
    <% } %>

    <form action="/login" method="POST">
      <select name="username" required>
        <option value="" disabled selected>Username</option>
        <option value="admin1">admin1</option>
        <option value="john_doe">john_doe</option>
      </select>

      <input 
        type="password" 
        name="password" 
        placeholder="Password" 
        required 
      />

      <button type="submit">Login</button>

      <p style="margin-top: 15px;">
        <a href="#">Forgot Password?</a>
      </p>
    </form>
  </div>

</body>
</html>
